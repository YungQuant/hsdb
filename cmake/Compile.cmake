OPTION(BITMEX "bitmex binary" OFF)
OPTION(EXAMPLE "example binary" OFF)

IF(BITMEX)
  ADD_EXECUTABLE(bitmex src/bitmex/trader.cpp)
  TARGET_LINK_LIBRARIES(bitmex PRIVATE ${REQUIRED_LIBRARIES})
  TARGET_COMPILE_DEFINITIONS(bitmex PRIVATE ${REQUIRED_DEFINITIONS})
  MESSAGE("-- BUILDING bitmex BINARY\t" ${GREEK_INCLUDE_DIRS})
ENDIF(BITMEX)

IF(EXAMPLE)
  ADD_EXECUTABLE(hsdb src/examples/hsdb.cpp)
  TARGET_LINK_LIBRARIES(hsdb PRIVATE ${REQUIRED_LIBRARIES})
  TARGET_COMPILE_DEFINITIONS(hsdb PRIVATE ${REQUIRED_DEFINITIONS})
  MESSAGE("-- BUILDING example BINARY\t" ${GREEK_INCLUDE_DIRS})
ENDIF(EXAMPLE)